export enum ButtonEnum {
    Primary = "primary",
    Secondary = "secondary",
    Purpure = "purpure",
}

export enum ButtonTypesEnum {
    Submit = "submit",
    Reset = "reset",
    Button = "button",
}

export enum SizesEnum {
    S = "s",
    M = "m",
    L = "l",
    XL = "xl",
}
